[package]
name = "probe"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true, features = ["derive"] }
thiserror = { workspace = true }

pnet = "0.35.0"
etherparse = "0.15"
aes = "0.8"
rand = { workspace = true }
link = { path = "../link" }
